# ============================================
# TEST ENVIRONMENT CONFIGURATION
# ============================================
# This file contains test-specific overrides
# Loaded automatically by pytest (see tests/conftest.py)
# ============================================

# Test Database (CRITICAL: Must end with '_test')
MONGODB_URI=mongodb://iris:Sveta87201120@localhost:27017/translation_test?authSource=translation
MONGODB_DATABASE=translation_test

# Disable external services during tests
EMAIL_ENABLED=false
GOOGLE_DRIVE_ENABLED=false

# Test Stripe Configuration
# Note: STRIPE_WEBHOOK_SECRET gets auto-set by validator when DB ends with '_test'
STRIPE_SECRET_KEY=sk_test_YOUR_TEST_KEY_HERE

# Rate limiting disabled for tests
RATE_LIMITING_ENABLED=false
