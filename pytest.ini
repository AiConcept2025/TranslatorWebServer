[pytest]
# Pytest configuration for MongoDB collection tests

# Test discovery patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Asyncio mode - AUTO for pytest-asyncio
asyncio_mode = auto

# Minimum Python version
minversion = 7.0

# Add current directory to Python path
pythonpath = .

# Test output options
addopts =
    -v
    --tb=short
    --strict-markers
    --disable-warnings

# Markers for organizing tests
markers =
    unit: Unit tests that don't require database
    integration: Integration tests that require database connection
    slow: Tests that take a long time to run
    collection: Tests for specific collections
    asyncio: Async tests

# Console output
console_output_style = progress

# Log options
log_cli = true
log_cli_level = INFO
log_cli_format = %(asctime)s [%(levelname)8s] %(message)s
log_cli_date_format = %Y-%m-%d %H:%M:%S

# Test discovery
testpaths = tests

# Timeout
timeout = 300
